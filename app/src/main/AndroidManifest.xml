<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shatha.mysidebar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".layoutfrag" />
        <activity android:name=".loc" />
        <activity android:name=".Aboutas" />
        <activity android:name=".Rateas" />
        <activity android:name=".Contectas" />
        <activity android:name=".Privacy" />
        <activity android:name=".Country" />
        <activity android:name=".F"></activity>
    </application>

</manifest>